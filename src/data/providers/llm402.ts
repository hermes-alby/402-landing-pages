import type { Provider } from '../types';

const provider = {
  key: 'llm402',
  name: 'llm402.ai',
  websiteUrl: 'https://llm402.ai',
  supportStatus: 'supported',
  sectionTitle: 'llm402.ai',
  sectionDescription: 'OpenAI-compatible AI endpoints gated by L402 so your agent can pay per request with no account or API key.',
} satisfies Provider;

export default provider;
