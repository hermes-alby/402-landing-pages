import type { Provider } from '../types';

const provider = {
  key: 'l402-services',
  name: 'L402 Services',
  websiteUrl: 'https://l402.services',
  supportStatus: 'supported',
  sectionTitle: 'L402 Services',
  sectionDescription: 'Paid intelligence endpoints that turn live market data into structured briefings your agent can use right away.',
} satisfies Provider;

export default provider;
